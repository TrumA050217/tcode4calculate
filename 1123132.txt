# 🧮 小学生算数系统 前端接口文档（v1.0）
**基础地址**：`http://localhost:8866`  
**版本**：1.0  
**说明**：除注册、登录外，其他接口均需携带请求头：  
```
token: 用户登录后返回的令牌
```

---

## 🧑‍💻 一、用户模块

### 1️⃣ 用户注册
**接口**：`POST /user/register`  
**功能**：注册新用户  

#### 请求体（application/json）
```json
{
  "username": "string",
  "password": "string"
}
```

#### 响应示例
```json
{
  "code": 200,
  "message": "注册成功",
  "data": true
}
```

---

### 2️⃣ 用户登录
**接口**：`POST /user/login`  
**功能**：账号密码登录  

#### 请求体
```json
{
  "username": "string",
  "password": "string"
}
```

#### 响应示例
```json
{
  "code": 200,
  "message": "登录成功",
  "data": "token字符串"
}
```

---

### 3️⃣ 小程序授权登录
**接口**：`GET /user/login/{code}`  
**功能**：根据微信授权 `code` 登录  

#### 路径参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| code | string | ✅ | 微信登录凭证 code |

#### 响应示例
```json
{
  "code": 200,
  "message": "OK",
  "data": "token字符串"
}
```

---

## 🧮 二、计算器接口

### 1️⃣ 生成题库
**接口**：`GET /calculate/generate`  
**功能**：生成新的题库  
**需要登录**

#### 响应示例
```json
{
  "code": 200,
  "message": "题库生成成功",
  "data": 101
}
```

---

### 2️⃣ 生成题目
**接口**：`GET /calculate/generate/questions`  
**功能**：为指定题库生成题目  
**需要登录**

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bankId | int64 | ✅ | 题库ID |
| type | int32 | ✅ | 题目类型（如1加法、2减法、3乘法、4除法） |
| quantity | int32 | ✅ | 生成题目数量 |

#### 响应示例
```json
{
  "code": 200,
  "message": "题目生成成功",
  "data": true
}
```

---

### 3️⃣ 手动生成题目
**接口**：`POST /calculate/manual`  
**功能**：手动指定题目添加进题库  
**需要登录**

#### Query 参数
| 名称 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bankId | int64 | ✅ | 题库ID |

#### 请求体
```json
[
  {
    "operandA": 5,
    "operandB": 7,
    "type": 1
  }
]
```

#### 响应
```json
{
  "code": 200,
  "message": "题目保存成功",
  "data": true
}
```

---

### 4️⃣ 提交作答
**接口**：`POST /calculate/submit`  
**功能**：提交答题结果  
**需要登录**

#### 请求体
```json
[
  {
    "bankId": 101,
    "recordId": 202,
    "myAnswer": 12.0
  }
]
```

#### 响应
```json
{
  "code": 200,
  "message": "提交成功",
  "data": true
}
```

---

### 5️⃣ 查询题库题目
**接口**：`GET /calculate/get`  
**功能**：查询指定题库下的所有题目  
**需要登录**

#### 参数
| 名称 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bankId | int64 | ✅ | 题库ID |

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    { "type": 1, "a": 5, "b": 6 },
    { "type": 2, "a": 9, "b": 3 }
  ]
}
```

---

### 6️⃣ 查询我的题库
**接口**：`GET /calculate/getMyBank`  
**功能**：查询当前用户创建的题库  
**需要登录**

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    {
      "bankId": 101,
      "createdBy": "小明",
      "createdAt": "2025-10-30T12:00:00"
    }
  ]
}
```

---

### 7️⃣ 查询用户作答结果
**接口**：`GET /calculate/result`  
**功能**：查询用户在某题库的所有答题结果  
**需要登录**

#### 参数
| 名称 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bankId | int64 | ✅ | 题库ID |

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    { "operandA": 5, "operandB": 7, "type": 1, "myAnswer": 12, "isCorrect": 1 }
  ]
}
```

---

### 8️⃣ 查询正确率与总答题数
**接口**：`GET /calculate/result/accuracy`  
**功能**：统计该题库的正确率和总答题数量  
**需要登录**

#### 参数
| 名称 | 类型 | 必填 | 说明 |
|------|------|------|------|
| bankId | int64 | ✅ | 题库ID |

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    {
      "total": 20,
      "accuracy": 0.85
    }
  ]
}
```

---

### 9️⃣ 查询错题本
**接口**：`GET /calculate/wrong`  
**功能**：查询用户的错题记录  
**需要登录**

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    { "operA": 8, "operB": 4, "mistakeType": 3 }
  ]
}
```

---

### 🔟 查询用户历史作答记录
**接口**：`GET /calculate/all`  
**功能**：查询所有历史答题记录  
**需要登录**

#### 响应
```json
{
  "code": 200,
  "message": "OK",
  "data": [
    { "operandA": 3, "operandB": 2, "type": 1, "myAnswer": 6, "isCorrect": 2 }
  ]
}
```

---

## 🔐 通用响应格式
```json
{
  "code": 200,
  "message": "请求成功",
  "data": {}
}
```

---

## 🚀 前端请求示例（Axios 版）
```js
import axios from 'axios';

const api = axios.create({
  baseURL: 'http://localhost:8866',
  headers: {
    token: localStorage.getItem('token') || ''
  }
});

// 用户登录
export function login(data) {
  return api.post('/user/login', data);
}

// 获取题库
export function getMyBank() {
  return api.get('/calculate/getMyBank');
}

// 生成题目
export function generateQuestions(params) {
  return api.get('/calculate/generate/questions', { params });
}
```